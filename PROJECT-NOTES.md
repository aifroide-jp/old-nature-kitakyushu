# 案件固有の申し送り（nature-kitakyushu）

**この案件だけに当てはまる注意事項・お客様に確認が必要なこと・運用上のリスク**を集める。

置き場所の使い分け:

| ファイル | 内容 |
|---|---|
| `.claude/ichiki/rules/ichiki.md` | 全案件共通の固定ルール |
| `CLAUDE.md` | この案件の**実装ルール**（こう作る、という決めごと） |
| **`PROJECT-NOTES.md`（本ファイル）** | この案件の**リスクと確認事項**（決まっていない・確認が必要・運用で気をつける） |
| `scripts/test-spec/KNOWN-LIMITATIONS.md` | テスト仕様書生成ツール固有の限界 |

---

## 1. 写真投稿フォーム：メール添付が届かない可能性がある

### 要件

`photos/submit.html` は「写真ファイル **最大5枚・各10MBまで**」と表記している。
運用フローは**メールで受け取り、管理者が内容を確認し、掲載するものを選んでサイトへ載せる**。

### リスク

**最大 50MB の添付メールになる。これは届かない可能性が高い。**

制限は3つ別々にあり、上げられるのは2つだけである。

| 制限 | 何を止めるか | 上げられるか |
|---|---|---|
| PHP の `upload_max_filesize` / `post_max_size` | ブラウザ→サーバーのアップロード | 上げられる（サーバー設定） |
| Contact Form 7 の `limit:` オプション | CF7 自身のファイルサイズ判定（既定 約1MB） | 上げられる（タグに書く） |
| **メールの添付上限** | **サーバーからの送信・管理者側の受信** | **こちらでは上げられない** |

メールサーバーの添付上限は一般に10〜25MB程度。送信側を通っても、**管理者の受信側で弾かれる**
（受信先が Gmail なら25MBが上限）。

### さらに悪い挙動

**Contact Form 7 は送信後に一時ファイルを削除する。** メール送信が失敗すると、投稿された写真は
サーバーにもメールにも残らない。

**投稿者の画面には「送信完了」が出るのに、管理者には何も届かない。**
これがこのリスクのいちばん厄介な点で、失敗しても誰も気づかない。

### 確認が必要なこと

1. 本番（エックスサーバー）の**送信時の添付上限**
2. **管理者の受信先**とその添付上限（Gmail 等なら25MB）
3. 実際に 50MB 相当を送って届くかの実測

### 現在の判断

**要件（5枚）を維持する。** 挙動を変えるほうが筋が悪いため。
複数ファイルアップロードのプラグインを追加して5枚受け付ける（下記2）。

上記の確認で届かないと分かった場合の選択肢:

| 案 | 内容 | 影響 |
|---|---|---|
| 枚数・容量を落とす | 例: 1枚・5MB | **要件変更**（お客様の合意が必要） |
| 仮アップロード方式に変える | 投稿を下書きで作り、管理画面で選んで公開 | **業務フローが変わる**（メールで見るのをやめる）。プラグイン追加は不要 |

仮アップロード方式のほうが長期的には軽い（メールの容量制約から解放され、審査が
「下書き一覧から選んで公開」になり、ダウンロード→再アップロードの手間が消える）。
ただし**お客様の仕事の進め方を変える提案**になるため、こちらからは決められない。

---

## 2. 追加プラグイン依存：CF7 の複数ファイルアップロード

### なぜ必要か

Contact Form 7 の `[file]` タグは **1タグ1ファイル**で、`multiple` 属性を出さない。

`photos/submit.html` のアップロード欄は、透明な `<input type="file" multiple>` を
ドロップエリア全面に重ねた形（JS なし）。**見た目を変えずに5枚受け付けるには、
CF7 の拡張が必要**である。

`[file]` を5つ並べれば拡張は不要だが、ドロップエリア1つという**デザインが変わる**。

### 確認が必要なこと

複数ファイルアップロードに対応する CF7 拡張プラグインは複数存在する
（ドラッグ&ドロップ対応のものが広く使われている）。ただし**選定時に必ず確認すること**:

- 現在もメンテナンスされているか（最終更新日）
- 使用する WordPress / PHP / Contact Form 7 のバージョンで動くか
- ドロップエリアのマークアップを自前で出すか（出す場合は CSS の当て直しが必要）

**プラグイン名をここに書き残さない。** 選定時点で状況が変わるため、そのとき確認して決める。

### 依存の合計

```
Advanced Custom Fields（無料版）
Safe SVG
Contact Form 7
＋ CF7 複数ファイルアップロード拡張   ← 写真投稿フォーム1枚のためだけ
```

**フォーム1枚のために依存が1つ増える**ことは認識しておく。上記1の判断が
「仮アップロード方式」に変われば、この依存は不要になる。

### 決まったら直す場所

現在、`photos/submit.html` を変換すると `multiple` で**意図的に停止する**（黙って1枚に
落とさないため）。判断が出たら次を直す。

**プラグインを入れる場合**

1. 本ファイル（上記）に選定したプラグイン名・バージョン・確認結果を記録
2. `proposal/converter/lib/gen/cf7.js` の `fieldTagFor()`（`type === 'file'` の分岐）に
   そのプラグインのタグ書式を追加
3. `proposal/vocabulary.md` 6.1節「ファイル欄」の `multiple` の記述を更新
4. `proposal/prompts/mockup-generation.md` の同じ記述も更新（3ファイル同期）

**複数枚を諦める場合**

1. モック `photos/submit.html` の `multiple` を外す
2. 同じページの表記「最大5枚」を直す（表記と挙動を一致させる）
3. 本ファイルに「枚数を落とした」経緯と合意日を記録

**仮アップロード方式に変える場合**

1. お客様の業務フロー変更の合意を取る（メールで見るのをやめる）
2. `data-cf7` ではなく別の宣言が必要になる。語彙の追加から検討
3. 本ファイルの上記1の「現在の判断」を書き換える

---

## 3. チェックボックス／ラジオは Contact Form 7 のマークアップになる

**モックと1:1にならない唯一の例外。**

CF7 はチェックボックス・ラジオのグループを、1つのタグから**自前のマークアップ**で出力する。

```html
<!-- CF7 の出力（変換器では制御できない） -->
<span class="wpcf7-form-control-wrap" data-name="interest">
  <span class="wpcf7-form-control wpcf7-checkbox">
    <span class="wpcf7-list-item"><label><input type="checkbox" …><span class="wpcf7-list-item-label">生態系保全</span></label></span>
```

モックは `<label><input> テキスト</label>` の並びで、`.radio-label` のような独自クラスを持つ。
**この構造は保てない。**

### 対処

**CSS を CF7 の構造に当て直す。見た目は再現する。**

- CF7 の出力に依存するスタイルは `assets/css/cf7.css` に分離する（`base.css` に混ぜない）
- **CF7 の出力構造が変わると崩れる箇所がここに集まる**ようにしておく

テキスト入力（`text` / `email` / `tel` / `textarea` / `select` 等）は CF7 が `class` と `id` を
引き継げるため、**1:1のまま保てる**。例外はグループ系だけ。

---

## 4. Contact Form 7 6.x のタグ属性順序

実装ルールは `CLAUDE.md` の「CF7 6.x 固有の注意事項」に記載。

**モック側では意識しない。** タグ生成は変換器が行うため、
`placeholder "…"` を末尾に置く等の順序制約は変換器の責務である
（人が書かないことで、既知の事故が構造的に起きなくなる）。

## 5. フォーム送信は「メールのみ」。応募一覧は作らない（2026-08-17 決定）

Contact Form 7 は**送信内容を DB に保存しない**。管理画面に応募の一覧は出ず、
届くのはメールだけである。一覧が欲しい場合は Flamingo（同作者の無料プラグイン）が要るが、
**要件に無いため入れない。**

### この決定と「イベント申込を1フォームにする」の関係

イベント申込は1つの CF7 フォームを全イベントで使い回す設計にした
（フォームがイベント数に比例して増えると、送信先・自動返信・メール本文を
1件ずつ管理することになり運用が破綻するため。vocabulary.md 6.2）。

**もし後から「応募を管理画面で見たい」が要件に入ると、この選択がトレードオフになる。**
Flamingo は「1フォーム＝1チャンネル」で一覧を分けるため、
1フォームだと**全イベントの応募が同じ箱に入る**。イベント別に見るには一覧側の
カスタマイズ（`parse_query` フィルタ等）が別途必要になる。

情報自体は失われない。フォームには `event-id` / `event-name` の hidden があり、
投稿のスラッグとタイトルが入る（vocabulary.md 6.2 の `data-cf7-value`）。
メール本文にも含まれるので、どのイベントの応募かは常に判別できる。

### 要件が変わったときに見るところ

- Flamingo を入れるか（一覧が要るか）
- 入れる場合、イベント別・開催前後での絞り込みが要るか
  → 要るなら一覧画面のカスタマイズが追加で必要。標準機能では絞れない

### 5.1 切り分け: 「応募・問合せを管理画面で見たい」が要件に入ったとき

現状（メールのみ）から変える場合に必要なものを、判断できる形で分けておく。

| 段階 | 必要なもの | 誰がやるか |
|---|---|---|
| ① 一覧を見られるようにする | Flamingo を入れる | setup で選択させる |
| ② フォーム別に分けて見る | **1フォームだと分かれない**。CF7 のフォーム＝Flamingo のチャンネルのため | — |
| ③ イベント別に絞り込む | Flamingo 一覧のカスタマイズ（`parse_query` 等）。**こちらで実装が要る** | 変換器 or テーマ |
| ④ 開催前後で絞り込む | ③に加えてイベント投稿の日付との突合。標準機能に無い | 同上 |

- ①は setup の設問にできる（プラグイン依存なのでモックからは決まらない）
- ②は1フォーム化とのトレードオフ。イベント別に見たいなら③が必須になる
- 情報は失われない。`event-id` / `event-name` の hidden に投稿のスラッグとタイトルが入る

**要件に入っていないので今は作らない。** 入った時点で①→③の順に判断する。
