# この案件の状態

> [案件のトップに戻る](../README.md)

**検証用の案件です。** モック→Ichiki→WordPress が機能するかを確かめるために作りました。

---

## 構造化が途中です

既存モック51ページのうち12ページだけを制約語彙に直した状態です。
`.ichiki.json` の `retrofit` に宣言してあり、その間は次のようになります。

- 未解決の内部リンクが警告に落ちる（構造化していないページへのリンク）
- 生成したテーマが管理画面に「変換途中です」と出す

`ichiki doctor` が構造化前／構造化済みのページ数を数えます。

### 合意デザインと比べる

構造化（`data-*` の付与）で**見た目が変わっていないか**を確かめるときだけ使います。
通常の検査（モック ↔ WordPress）は `ichiki deliver` に入っているので、これは特殊な場合です。

```bash
node .claude/ichiki/bin/ichiki.js serve .ichiki/mockup-before 18081 &
node .claude/ichiki/bin/ichiki.js diff . http://localhost:18081
```

`.ichiki/mockup-before/` は**合意デザイン**です。見た目はお客様と合意済みで、
宣言（`data-*`）だけが付いていない状態のものです。

---

## やらないと決めたこと

**残り39ページのモック化はやりません。** 既存モック51ページのうち12ページだけを
制約語彙に変換した状態で打ち止めです。検証の目的は12ページで足りています。

したがって次は**残件ではなく、恒久的にこのまま**です。

| | |
|---|---|
| 未解決リンク28件 | 残る。`.ichiki.json` の `retrofit` 宣言で許可し続ける |
| 管理画面の「変換途中です」警告 | 出続ける。検証サイトなので問題ない |
| `verify:live` の `nav-short` 1件 | 残る。未変換ページぶんのナビが出ないため |
| `retrofit` 宣言 | 消さない |

`ichiki doctor` が変換前／変換済みのページ数を出します。

---

## 既知で許容している差

**直さないと決めたもの**と**まだ特定していないもの**があります。
`gate` や `deliver` がこれで落ちますが、想定内です。

| | 件数 | 扱い |
|---|---|---|
| a11y コントラスト（AA違反） | 123 | 元モック由来。色とテキストの重なり。**デザイン判断**なので変換では直せない |
| `verify:live` の nav-short | 1 | 未変換39ページぶんのリンクが出ない。上記のとおり残り続ける |
| ピクセル差 index | 253px | 未特定。フォントの太さ由来と見ているが切り分けていない |
| ピクセル差 biodiversity | 249px | 同上 |
| ピクセル差 events-index | 137px | **元サイトが一覧ページで現在地を出していない不整合。** こちらを一貫させた結果なので戻さない |
| ピクセル差 contact | 29px | CF7 のマークアップ。同意チェックが `<span class="wpcf7-list-item">` に包まれる（`CLAUDE.md` の「モックと1:1にならない唯一の例外」） |
| ピクセル差 center / center/biotope | 2872px / 2837px | ヒーローを `background-image` から `<img>` にしたため。**画像を ACF で差し替え可能にするための必要な構造変更** |

> 一覧ページの差異率が大きく出る（`/about/spots/` 62% など）のは、モックが見本を
> 何枚も並べているのに対し実サイトは実際の投稿数しか出さないためです。
> レイアウトの不具合ではありません。

---

## 出力の凍結を外すとき

改修が落ち着いたら外します。そのとき消すのは**この4つ**です。忘れると死んだ検査が残ります。

| 消すもの | 何か |
|---|---|
| `.ichiki/snapshot.json` | 期待値（96ファイルのハッシュ） |
| `.claude/ichiki/src/snapshot.js` | 本体 |
| `.claude/ichiki/bin/ichiki.js` の `snapshot` 行 | コマンド登録 |
| `.claude/ichiki/src/gate.js` の `--snapshot` と「出力の凍結」ステップ | gate からの呼び出し |

`package.json` の `test:update` は `test/run.js`（scan の回帰）で**別物です。残します。**

---

## 後回しにしているもの

Ichiki 側の話です。

- `/mockup` と `/retrofit`（AI が要るコマンド。Skill にする方針）
- 制約に縛られない Ichiki テスト用 fixture と、そこから逆算した `mockup-bad`
- `publish-mockup --zip`（お客様への HTML 直渡し用）
- `ichiki serve --all`（開発者が旧モック・モック・WP を見比べる用）

---

## acf-map.yaml は何のためにあるか

**yaml だけではテーマを作れません。** マークアップの骨格は yaml に無く、モックの HTML にしか
ありません（yaml が持つ HTML は wysiwyg のデフォルト値だけ）。入力元は常にモックで、
yaml はその読み取り結果の記録です。直したいときはモックを直して `scan` を回し直します。

それでも残すのは2つの理由です。

- **人が読める解釈記録になる。** 変換器がモックをどう読んだかが残らないと、生成物を読むまで
  読み違いに気づけません。お客様と合意するのもこの台帳です
- **検収成果物の入力になる。** `ichiki testspec` が C1 / C3 をここから組み立てます

読み取りの実装は変換器1つで、`scan` はそのモデルを yaml に書き出すだけです。
`convert --acf-map <yaml>` で台帳と生成物を突き合わせ、1件でも食い違えば停止します。
