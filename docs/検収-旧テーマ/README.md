# 検収成果物（旧テーマ版・2026-08-19 生成）

**人手（AI）で PHP を書いていた頃のテーマ**に対して出したもの。
入力は当時の `acf-map.yaml`（制約語彙より前の形式）と
`old-nature-kitakyushu`（localhost:10004）のテーマ。

いまのリポジトリはモックが制約語彙版に入れ替わっており、
`ichiki testspec` は `docs/検収/` にそちら向けのものを出す。
**これは再生成できない**（入力の台帳もテーマもリポジトリから消えている）ので記録として残す。

| | |
|---|---|
| `test-spec.md` | C1 テスト仕様書 |
| `l1-checklist.tsv` | C3 検収シート |
| `l1-guide.md` | C3 付録（L1向けガイド） |
