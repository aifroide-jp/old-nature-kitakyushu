# 本番リリース手順書 — nature-kitakyushu

<!-- ichiki release で自動生成。手で編集しても次回の生成で消えます -->

上から順に実行してください。**「要記入」と書かれた箇所は、実行前に埋める必要があります。**
サーバやドメインの情報は機械が知りようがないため、空欄にしてあります。

> **この文書は「本番サーバに載せて設定する」ための手順です。**
> サイトの中身が正しいか（見た目・文言・レスポンシブ・フォームの動作）は
> **検収**の役目で、ここでは扱いません。
>
> - [テスト仕様書（C1）](検収/test-spec.md) … 何をどう確認するか
> - [検収シート（C3）](検収/l1-checklist.tsv) … 目視で確認して判定を書き込む表
> - [検収ガイド](検収/l1-guide.md) … 判定に迷ったときの読みもの
>
> 同じ確認を2つの文書に書くと、片方だけ直されてズレるためです。
> ここで見るのは**本番環境でしか起きないこと**だけです。

> **注意: このテーマは変換途中のモックから作られています。**
> 行き先の無いリンクが残ります。**検収で確認してから公開してください。**
> 管理画面にも警告が出ます。

## 0. 事前に用意するもの（要記入）

| 項目 | 値 |
|---|---|
| 本番ドメイン | |
| サーバ（エックスサーバー）のアカウント | |
| FTP / SSH の接続先 | |
| WordPress の管理者アカウント | |
| 公開予定日 | |

前提:

- WordPress 6.5 以上 / PHP 8.1 以上 / クラシックテーマが動く状態
- [ ] **検収が済んでいる**（[検収シート](検収/l1-checklist.tsv) の目視確認が終わっている）

## 1. プラグインを入れて有効化する

SSH が使えるなら1行で済みます。

```bash
wp plugin install advanced-custom-fields contact-form-7 safe-svg --activate
```

管理画面から入れる場合は「プラグイン > 新規追加」で次を検索して有効化します。

- [ ] advanced-custom-fields
- [ ] contact-form-7
- [ ] safe-svg

**Advanced Custom Fields は無料版で構いません**（PRO 専用機能は使っていません）。

## 2. テーマを入れて有効化する

テーマ名: `nature-kitakyushu`

テーマは手元で作ります（本番サーバでは作りません）。

```bash
node .claude/ichiki/bin/ichiki.js build . <出力先>/nature-kitakyushu
```

1. [ ] 出来た `nature-kitakyushu` フォルダを zip にする
2. [ ] 「外観 > テーマ > 新規追加 > テーマのアップロード」で入れる（FTP で置いてもよい）
3. [ ] 有効化する

検証環境（Local）で使っているテーマをそのまま持っていっても構いません。
中身は同じものです。

## 3. 初期データを入れる

テーマを有効化したあと、**管理画面を1回開くだけ**です。
固定ページ・投稿・フォーム・メニュー・サイト名が自動で作られます。
パーマリンクの設定もテーマ有効化時に自動で反映されます（手で保存し直す必要はありません）。

- [ ] 管理画面（`/wp-admin/`）を開く

サイト名「アーバンネイチャー北九州」／キャッチフレーズ「都市と自然、近いからこそおもしろい。」 が入ります。
メニューは `global` と `footer` の位置に作られます。

## 4. フォームの送信先を設定する（要記入）

**ここは必ず手で設定してください。** 既定では WordPress の管理者メールアドレスに届きます。

| フォーム | 送信先メールアドレス（要記入） |
|---|---|
| contact | |
| event-apply | |

「お問い合わせ」からフォームを開き、「メール」タブの「送信先」を書き換えます。

- [ ] 送信先を設定した
- [ ] **本番サーバから実際に送信して、受信できた**

フォームが動くかどうかは検収で確認済みです。ここで見るのは**本番のメールサーバで
実際に届くか**で、これは環境が変わると結果が変わるため、本番でもう一度必要です。

## 5. 動いているか確かめる

**コマンドで確かめられます。** 全ページを開いて、宣言どおりに出ているかを見ます。

```bash
node .claude/ichiki/bin/ichiki.js verify:live . <本番のURL>
```

- [ ] 不一致が0件（または残っているものが検収で確認済み）

見た目や文言の確認は検収で済んでいます。ここで見るのは
**サーバの設定やパーマリンクの反映漏れで 404 になっていないか**です。
コマンドが使えない場合は、下の一覧から種別ごとに1つずつ開いてください。

<details><summary>ページの一覧</summary>

| URL | 内容 | テンプレート |
|---|---|---|
| `/` | トップページ | `front-page.php` |
| `/about-biodiversity/` | 固定ページ | `page-about_biodiversity.php` |
| `/about/spots/` | 北九州市の自然スポット の一覧 | `archive-nkk_spot.php` |
| `/about/spots/<記事のスラッグ>/` | 北九州市の自然スポット の詳細 | `single-nkk_spot.php` |
| `/center/` | 活動拠点 の一覧 | `archive-nkk_center.php` |
| `/center/<記事のスラッグ>/` | 活動拠点 の詳細 | `single-nkk_center.php` |
| `/events/` | イベント の一覧 | `archive-nkk_event.php` |
| `/events/<記事のスラッグ>/` | イベント の詳細 | `single-nkk_event.php` |
| `/events/<記事のスラッグ>/apply/` | イベント のapply | `single-nkk_event-apply.php` |
| `/news/` | お知らせ の一覧 | `archive-nkk_news.php` |
| `/news/<記事のスラッグ>/` | お知らせ の詳細 | `single-nkk_news.php` |
| `/contact/` | 固定ページ | `page-contact.php` |

</details>

## 6. 画像を入れる

初期データと一緒にメディアライブラリへ登録されます。差し替えたい画像は
管理画面の各ページの編集欄から選び直してください。

## 7. 公開前の最終確認

本番環境でしか確認できないことだけを挙げます（中身の確認は検収の役目）。

- [ ] 検索エンジンによるインデックスを許可した（「設定 > 表示設定」のチェックを外す）
- [ ] SSL（https）で表示される
- [ ] 管理者アカウントのパスワードを本番用にした

