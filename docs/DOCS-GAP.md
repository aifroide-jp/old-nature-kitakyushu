# 必要な手順書・成果物と、現状の差

読み手が誰かで性質が変わるので、そこで分けて並べる。

計測日: 2026-08-18 / 見直し: 2026-08-21

## いま残っているもの（2026-08-21）

**成果物**

- #1 #2 #4 手順書（本体に1つ置く）

**合意のうえ後回し**

- `/mockup` と `/retrofit`（下の節を参照）
- 制約に縛られない Ichiki テスト用 fixture と、そこから逆算した `mockup-bad`
- snapshot の凍結解除

**やらないと決めたこと**

- **残り39ページのモック化はやらない。** この案件は12ページで打ち止め。
  検証の目的（モック→Ichiki→WordPress が機能するか）は12ページで足りている。
  したがって以下はこの案件では**恒久的にこのまま**であり、残件ではない。

  | | この案件での扱い |
  |---|---|
  | 未解決リンク28件 | 残る。`.ichiki.json` の `retrofit` 宣言で許可し続ける |
  | 管理画面の「変換途中」警告 | 出続ける。検証サイトなので問題ない |
  | `verify:live` の nav-short 1件 | 残る |
  | `retrofit` 宣言 | 消さない |

  **モックの置き場所の是正は 2026-08-24 に実施済み。** 39ページとは無関係にできた。
  12ページをルートへ、旧51ページを `.ichiki/mockup-before/` へ移し、規約どおりの形になった。
  `retrofit` 宣言は残したまま。

**`proposal/` は無くなった（2026-08-24）**

「`proposal/` を全部『出力されるもの』で構成する」という当初の目標は、
**`proposal/` 自体を畳む**ことで終わった。行き先はこう。

| かつて | いま |
|---|---|
| `proposal/mockup-real/`（制約版12ページ） | **リポジトリのルート**。規約（`rules/ichiki.md`「モックの置き場所」）どおり |
| ルート（旧51ページ） | `.ichiki/mockup-before/`。リバース前の記録 |
| `proposal/snapshot/expected.json` | `.ichiki/snapshot.json` |
| `proposal/visual-check/` | 削除。`pairs.json` はどこからも読まれておらず、`compare.js` は `diff.js` と同じことをしていた |
| `proposal/TOOLS.md` `proposal/DIFF-vs-ichiki.md` | 削除。移設前の記録で、内容は実現済みか反転していた |
| `proposal/DOCS-GAP.md` | このファイル（`docs/` へ） |
| `proposal/README.md` | ルートの `README.md` へ |
| ルートの `field-map.json` | 削除（2026-08-24）。295件すべて `source === target` の恒等写像で、誰も読んでいなかった。宣言名がそのままフィールド名になるので、機械命名→意味名のリネーム工程自体が無い |
| ルートの `scripts/` | 削除（2026-08-24）。中身は `.DS_Store` だけ。test-spec は `ichiki testspec` へ移設済み |

`mockup-real` を「出力されるもの」にはしなかった。作るのが AI なので再現せず、
一度きりの変換結果が新しい正になったものだから（ビルド生成物ではない）。

### 凍結（snapshot）を外すとき

改修が落ち着いたら外す。そのとき消すのはこの4つ。**忘れると死んだ検査が残る。**

| 消すもの | 何か |
|---|---|
| `.ichiki/snapshot.json` | 期待値（96ファイルのハッシュ） |
| `.claude/ichiki/src/snapshot.js` | 本体 |
| `bin/ichiki.js` の `snapshot` 行 | コマンド登録 |
| `src/gate.js` の `--snapshot` と「出力の凍結」ステップ | gate からの呼び出し |

`package.json` の `test:update` は `test/run.js`（scan の回帰）なので**別物。残す**。

## 既知で許容している差（2026-08-21 実測）

**直さないと決めたもの**と**まだ特定していないもの**を分ける。数字は測り直したもの。

| | 件数 | 扱い |
|---|---|---|
| a11y コントラスト（AA違反） | 123 | 元モック由来。色とテキストの重なり。**デザイン判断**なので変換では直せない |
| `verify:live` の nav-short | 1 | `/about-biodiversity/` のナビが14本（モックは25本）。未変換39ページぶんのリンクが出ない。39ページを作らないので**残り続ける** |
| ピクセル差 index | 253px | 未特定。フォントの太さ由来と見ているが切り分けていない |
| ピクセル差 biodiversity | 249px | 同上（フォント要因） |
| ピクセル差 events-index | 137px | **元サイトが一覧ページで現在地を出していない不整合。** こちらを一貫させた結果なので戻さない |
| ピクセル差 contact | 29px | 未特定 |
| ピクセル差 center/biotope・center | 2872px / 2837px | ヒーローを `background-image` から `<img>` にしたことによる暗幕の合成差。**画像を ACF で差し替え可能にするための必要な構造変更** |

---

## 一覧

| # | 必要なもの | 読み手 | 現状 | 出どころ |
|---|---|---|---|---|
| 1 | Ichiki セットアップ手順書 | 職員 | **△ 開発者向けの README しかない** | `.claude/ichiki/README.md`（構成説明・CLI仕様） |
| 2 | AI使用のモックアップ作成手順書 | 職員 | **△ AI に渡すプロンプトはある。人向けの手順が無い** | `.claude/ichiki/prompts/mockup-generation.md`（AI宛て） |
| 3 | C1 人的検証項目の出力 | 開発 | **○ ある** | `docs/検収/test-spec.md` |
| 4 | C1 Ichiki実行・検証手順書 | 職員 | **× 無い** | — |
| 5 | C2 リリース手順書の作成 | 職員 | **○ ある（2026-08-24）** | `ichiki release` → `docs/リリース手順書.md` |
| 6 | C3 L1向け検収ガイド | L1 | **○ ある（2026-08-24）** | `docs/検収/l1-guide.md` と `l1-guide.html`（絵つき） |
| 6b | C3 検収シート | L1 | **○ ある** | `docs/検収/l1-checklist.tsv`（15KB） |

**○ 4件 / △ 2件 / × 1件。**

出力先は `ichiki testspec` が決める（既定 `docs/検収/`、`.ichiki.json` の
`testspec.out_dir` で上書きできる）。移設前は `scripts/test-spec/out/` だった。

---

## 性質で分けると

### A. 生成物（機械が毎回作る）

`acf-map.yaml` とテーマとライブサイトを入力に、案件ごとに中身が変わるもの。

- C1 テスト仕様書（#3）… **実装あり**
- C3 検収シート（#6b）… **実装あり**
- C3 検収ガイド（#6）… 実装あり。**html 化と画像が無い**
- C2 リリース手順書（#5）… **実装なし**

### B. 手順書（人が読む。案件ごとにほぼ変わらない）

Ichiki 本体に1つ置いて、案件では原則そのまま使うもの。

- Ichiki セットアップ（#1）
- モックアップ作成（#2）
- Ichiki 実行・検証（#4）

**B は「案件ごとに生成する」必要が無い。** 本体に置いて参照すればよく、
案件固有の値（サイトURL・テーマ名）だけ差し込めばよい。

---

## C1 の自動判定を全部埋める手順（2026-08-24）

**`ichiki deliver` が全部やります。** 下は中で何が動いているかの説明。

C1 の6種別のうち4つは自動判定できる。**レポートを置けば取り込まれる**（無ければ「未実行」のまま）。

```bash
ichiki deliver                  # 下の4つを正しい順で流す
#   verify:live → diff --both → a11y --site → testspec → release
```

順番に依存がある。`diff` と `a11y` を先に回さないと `testspec` が「未実行」で出る。
**欠けても止まらず、欠けたまま成果物が出る**ので、手で順番を守る設計にしていない。

`.ichiki.json` に出力先を書く。

```jsonc
"testspec": { "visual_report": "docs/visual" }
```

実測: これで「未実行」が12件 → 0件になった（残るのは凡例の文中だけ）。

### アクセシビリティは pa11y-ci を入れずに済ませる

C1 は pa11y-ci の形式（`{ results: { <url>: issue[] } }`）を読む。
`ichiki a11y --site` が**同じ判定器の結果をその形で書き出す**ので、pa11y-ci は要らない。
判定器を2つ持つと結果が割れるため、あえて1つに寄せている。

## C1 の見た目チェックについて（2026-08-24）

`.ichiki.json` の `testspec.visual_report` に `ichiki diff` の出力先を書くと、
C1 の「表示確認」「レスポンシブ」に**実際の差異率が入る**（書かなければ「未実行」のまま）。

```jsonc
"testspec": { "visual_report": "docs/visual" }
```

```bash
ichiki diff . http://localhost:10009 docs/visual --both   # 撮って比べる
ichiki testspec                                            # 差異率を取り込む
```

### レポートは人が見るもの、results.json が機械が読むもの

`ichiki diff` は出力先に2つ置く。

| | 誰が読むか |
|---|---|
| `index.html` | **人**。日本語。表示は自由に変えてよい |
| `results.json` | **機械**。`testspec` が差異率を取り込む |

以前は `testspec` が `index.html` を正規表現で読んでいた。**表示を直したら壊れる。**
実測: 列見出しを日本語にしただけで viewport の照合が効かなくなった。

### 差異率をそのまま読んではいけない

一覧ページはモックがサンプルを何枚も並べているのに対し、WordPress は実際の投稿数しか出さない。
ページの丈が変わって以下が全部ずれるので、差異率が跳ね上がる。

| ページ | 差異率 | 原因 |
|---|---|---|
| `/about/spots/` | 62.53% | モック9枚（`data-loop-sample` 8件）vs 投稿1件 |
| `/events/` | 48.08% | 同上（サンプル5件） |
| `/center/` | 46.89% | 同上（サンプル9件） |
| `/contact/` | 21.84% | **CF7 のマークアップ**。同意チェックが `<span class="wpcf7-list-item">` に包まれてレイアウトが変わる（`CLAUDE.md` の「モックと1:1にならない唯一の例外」） |
| その他 | 0.86〜8.55% | フォント要因・ヒーローの `<img>` 化など |

一覧ページには C1 の出力に注釈を入れてある（数字だけ見せると「6割壊れている」と読まれるため）。
**`/contact/` の 21.84% は既知の例外**で、直すには CF7 のマークアップに CSS を合わせる必要がある。

---

## 誰が何を見るか（前提）

読み違えていたので明記する。**L1 は社内スタッフ。お客様ではない。**

| | 見るもの |
|---|---|
| **お客様** | 合意までの**モック** / 納品後の**WordPress**。この2つだけ |
| **社内スタッフ（L1）** | 検収シート・C1・検収ガイド。お客様は触らない |

したがって検収まわりの成果物は全部**社内向け**。
お客様に見せる文書ではない（とはいえ読みやすさは要る。検収は目視作業なので）。

## 検収の「見本」は手元のモック（2026-08-25）

検収するのは社内スタッフで、**リポジトリを落として作業する**。
したがってモックは手元にファイルとしてある。検収シートの「合意したデザイン」列は
`file://` でそこを指す。

| ページ | 実際のページ | 合意したデザイン |
|---|---|---|
| 生物多様性とは？ | `http://…/about-biodiversity/` | `file:///…/about/biodiversity.html` |

一度は公開サイトの配下に置く形にしたが（`ichiki publish-mockup`）、
**それは「L1 ＝ お客様」と読み違えていたための設計**だった。
社内スタッフが見るなら置く必要は無い。置くと41MBが公開サイトに乗り、消し忘れの管理も要る。

`publish-mockup` 自体は残す。**相手はお客様、局面は合意前。**
渡し方は「HTML を直渡し」か「検証サーバに設置」の2つで、後者がこれ。
置き先は必ず指定させる（既定を持たない。案件ごとに違うため）。

### ガイドから全ページの絵を外した

当初は全11ページ分の絵をガイドに埋めていた（22枚・4.7MB）。冗長だった。

- `ichiki diff` の比較レポートが既に全ページのモックと実サイトを並べている
- モックを置けば実物を見比べられるので、絵そのものが要らない
- 「使い方」と「見本」は別の仕事。混ぜると使い方が読まれなくなる

ガイドは使い方だけにして、絵は**見比べ方の例として1ページ分**だけ残した（4.7MB → 750KB）。

## #6 検収ガイドの HTML（実装済み 2026-08-24）

`ichiki testspec` が `l1-guide.html` も出す（別コマンドにすると呼び忘れるため、md と一緒に出す）。

**見せるのは「合意したデザイン」＝モック側の絵。** 実サイト側の絵ではない。
確認してほしいのは「実サイトがこの絵の通りか」なので、実サイトの絵を見せると
「絵と同じですね」で終わる（絵も壊れていれば一致してしまう）。

1ファイルで渡せるように、絵は幅480pxに縮めて JPEG で埋め込む。
原寸を全部入れると 40MB になり、メールでもチャットでも送れない（→ 4.7MB）。
`ichiki diff` を回していなければ絵なしで出る（4KB）。

一覧ページは**モックのサンプル件数と実際の投稿数が違う**ので、
「数の違いは問題ではありません」と本文に注記してある。
これを書かないと「絵と枚数が違う」で NG が付く。

---

## #5 リリース手順書（実装済み 2026-08-24）

`ichiki release` が `docs/リリース手順書.md` を出す。入力はモックと `.ichiki.json` だけで、
テーマは見ない（見に行くと「テーマを解析して推測する」古い作りに戻るため）。

機械が埋めるもの: プラグイン一覧 / テーマ名 / 全ページの URL とテンプレート /
メニューの位置 / フォーム名 / サイト名とキャッチフレーズ / **行き先の無いリンク**。

**検収（C1/C3）とは役割を分ける。** 手順書は「本番サーバに載せて設定する」、
検収は「サイトの中身が正しいか」。同じ確認を2つの文書に書くと片方だけ直されてズレるので、
表示確認・レスポンシブ・フォームの動作は手順書に書かず、**お互いへのリンクを張る**。
例外はフォームの着信確認で、本番のメールサーバでしか結果が分からないため両方でやる。

機械が知りようのないもの（ドメイン・FTP・DB・公開日・フォームの送信先）は
**空欄と「要記入」**で出す。それらしい値を書くと、確認せずに使われるため。

### 行き先の無いリンクは手順書に載せない（2026-08-25）

当初は32種類を列挙していた（56行）。**リリースの手順ではない。**

| | どこが見ているか |
|---|---|
| 本文のリンク切れ | C1 の「リンク遷移」。全ページ自動判定 |
| ナビの欠落 | `verify:live` の `nav-short` |

どちらも検収の側の仕事で、手順書に書くと同じことが2箇所になる
（表示確認・レスポンシブで同じ判断をした）。
公開してよいかどうかは0章の「検収が済んでいる」で担保する。

なお、数が食い違う理由は記録として残す。実測で lint L30 は32件、変換器は28件だった。
ナビの項目は参照型で登録するため、行き先が無いと**登録されずに黙って落ちる**
（変換器は報告しない）。`verify:live` の `nav-short`（25本→14本）はこれが原因。

---

## 別件: data-section が編集画面の区切りに使われていない（**解決済み 2026-08-20**）

> **決着**: `data-section-label` を語彙に足し、`inc/acf-<slug>.php` の生成時に
> `array('type' => 'tab', 'label' => <ラベル>)` を挟むようにした。
> 日本語ラベルの出どころは**宣言**にした（見出しテキストからの推測はしない）。
> 実測: event 10タブ / front 10タブ / center 7タブ。66件が平坦に並ぶ状態は解消。
> 以下は当時の記録。

`rules/ichiki.md` の食い違いを直す過程で見つけた**実装の抜け**。ルールの矛盾とは別。

旧方式は ACF の編集画面を3タブ（メインコンテンツ / セクション別 / 装飾要素）に分けていた。
振り分けは `HERO_RE = /(hero|main|top|kv|mv)/i` の部分一致で、これは推測なので削除した。

代わりに `data-section` があり、**実際のセクション名で分かれるので推測より正確**。
ところが生成された ACF に**タブフィールドが1つも出ていない**（`'type' => 'tab'` が0件）。

```
event の66フィールドが仕切りなしで平坦に並ぶ
hero_image / event_status / tag_1 / tag_2 / tag_3 / tag_4 / hero_title / event_date / …
```

情報は acf-map.yaml の `sections[].id` に入っているのに、使っていない。
**66件を仕切りなしで並べるのは L1 にとって辛い。** 検収の前に直したい。

やること: `inc/acf-<slug>.php` の生成時、セクションの切り替わりで
`array('type' => 'tab', 'label' => <セクション名>)` を挟む。

注意: セクション名は ASCII（`hero` / `detail` / `schedule`）なので、
**そのままタブ名にすると L1 に読めない**。日本語ラベルをどこから取るかは要検討
（モックの見出しテキストを使う案があるが、宣言が無い情報を推測することになる）。

---

## 後回し: AI が要るコマンドは /mockup と /retrofit（未着手）

> **その後（2026-08-21）**: `/run` は廃止した（変換がコマンド化され、AI の出番が消えたため）。
> `prompts/mockup-generation.md` は 530行 → 121行にして規約を持たせず、
> `rules/vocabulary.md` を先に読ませる形にした。
> 実装形態は**スラッシュコマンドより Skill** が有力。理由は起動条件で、
> `/mockup` は人が打つのを覚えている必要があるが、Skill は description が
> 合えば自動で入る（モック編集は合意まで何度も起きるので、後者が向く）。
> Skill 本体に規約は持たせず、案件の submodule を読ませる。そうすればコピーが無く、
> バージョンがズレない。**ただし自動起動は保証ではないので、正しさの担保は lint。**

**設計が反転していた。** Ichiki が用意していた `/setup` `/run` はどちらも機械的な
コマンドの羅列で、AI が判断する箇所が無かった。一方**本当に AI が要る仕事**である
「モックを書く」にはコマンドが無く、`prompts/mockup-generation.md`（530行）を
手で貼る運用だった。

| | 入力 | 出力 |
|---|---|---|
| `/mockup` | 要件（案件情報） | lint が通るモック |
| `/retrofit` | 制約なしモック | lint が通るモック（見た目を変えない） |

出力が同じで入力が違うだけなので、1コマンドの2モードでもよい。

### プロンプトが膨らむ問題への対処

語彙が育つほどプロンプトが膨らみ、1回で全部守るのが難しくなる
（実測: L31 と data-nav-current を足しただけで2節増えた）。

**巨大なプロンプトを1回渡す形にしない。lint が通るまで回す形にする。**

```
書く → ichiki lint → 指摘を直す → 通るまで繰り返す
```

これならプロンプトの網羅性への依存が下がる。今日の後付け実測（1ページ7分）が
この形で、lint の指摘がそのまま作業リストになった（L05 の53件は一括で処理できた）。

### 局面が2つある。混同しないこと

**モックは合意までに何度も触る。** 凍結物として扱うと設計を誤る。

| | 見た目 | 宣言 | 効く検査 |
|---|---|---|---|
| **合意前**（何度も触る） | **変わるのが正しい** | 保たれ続けないと困る | **lint だけ** |
| **後付け**（retrofit） | 変わってはいけない | これから付ける | lint ＋ ピクセル比較 |

合意前にピクセル比較をしても意味が無い（見た目を変えるのが目的だから）。
逆に後付けでは見た目が変わったら失敗なので、比較が絶対に要る。

**本当のリスクは「触るたびに宣言が腐る」こと。**
セクションを足して `data-section` を忘れる、文言を差し替えて `data-acf` を落とす、
`<head>` をいじって `<title>` を巻き込む。どれも静かに起きる。

実測: 後付け作業中に `<title>` を消した。**lint は通っていた**（a11y が捕まえた）。
lint だけでは足りない場面もある。

### だから /mockup は「1回生成するコマンド」ではない

```
要件 → 書く → lint → 見せる → 直す → lint → 見せる → … → 合意 → 変換
                ↑______________________|
                  ここが何周も回る
```

**触るたびに検査が走る形にする。** lint は 51ページで 0.42 秒（実測）なので、
編集ループに毎回入れても邪魔にならない。

未検討: 職員やデザイナーが HTML を直接編集する場合はコマンドを通らない。
pre-commit フックなど、**編集経路に依存しない止め方**が要るかもしれない。

### 副次的な利点

- 「vocabulary.md を読んでね」と毎回言わなくて済む（コマンドが読ませる）
- リポジトリ直下にモックが作られる（`.ichiki.json` の `mockup: "./"` と揃う）
