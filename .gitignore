# 環境依存の設定（絶対パスを含むためコミットしない）
.ichiki.json

# OS / エディタ
**/.DS_Store
.claude/settings.local.json

# 生成物（コマンドで再生成できるもの）
# 例外: proposal/snapshot/expected.json は凍結の期待値なので追跡する
docs/検収
proposal/visual-check/out
pa11y-report.json
.pa11yci.json
