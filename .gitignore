# 環境依存の設定（絶対パスを含むためコミットしない）
.ichiki.json

# OS / エディタ
**/.DS_Store
.claude/settings.local.json

# 生成物（コマンドで再生成できるもの）
# 例外: .ichiki/snapshot.json は凍結の期待値なので追跡する
docs/検収
# ピクセル比較のスクリーンショット（ichiki diff で再生成できる。数十MB）
docs/visual
pa11y-report.json
.pa11yci.json

# ichiki scan のレポート（acf-map.yaml と違い、合意の対象ではない）
coverage.json
